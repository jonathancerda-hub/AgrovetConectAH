# Configuración del Backend
NODE_ENV=production
PORT=10000

# Base de Datos Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Base de Datos PostgreSQL (Supabase)
PGHOST=
PGPORT=5432
PGDATABASE=postgres
PGUSER=
PGPASSWORD=

# JWT Secret (GENERAR UNO NUEVO CON: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=
JWT_EXPIRES_IN=24h

# CORS Origin (URL del frontend)
CORS_ORIGIN=
